server:
  port: 8081


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotelmicroo?createDatabaseIfNotExist=true
    username: root
    password: Harshada@0893
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
 
    
